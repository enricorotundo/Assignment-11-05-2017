app.controller("HomepageController", [
    "$scope",
    "$location",
    "$log",
    function ($scope, $location, $log) {
        'use strict';
        $log.debug("Homepage Controller Initialized");
    }]);